/** Bottom clearance for the floating embed toolbar (bottom-6 + pill height). */
export const STUDIO_EMBED_MENU_BAR_CLEARANCE = 72;

/** Default iframe height — canvas plus floating toolbar clearance. */
export const STUDIO_EMBED_DEFAULT_HEIGHT = 540;
