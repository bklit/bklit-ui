---
title: Tabs
description: StudioTabs segmented controls — segmented, icons, cards, legend grid
---

`StudioTabs` / `StudioTab` wrap shadcn `ToggleGroup` with `variant="studio"` and layout presets from `STUDIO_TABS_LAYOUTS`.

**Source:** `packages/studio/src/components/controls/studio-toggle-group.tsx`

<ComponentShowcase>
  <StudioUiPreview>
    <TabsDemo />
  </StudioUiPreview>
</ComponentShowcase>

## Layouts

| Layout | Use case |
|--------|----------|
| `segmented` | Text pills (Fill mode, Ease/Spring) |
| `icons` | Icon row (orientation, line cap, fade edges) |
| `cards-2` | 2-column preview cards (ease presets, curves) |
| `cards-3` | 3-column preview cards |
| `swatch` | Swatch + label (pie fill) |
| `legend` | 3×2 placement grid |

`IconToggleGroup` is a thin wrapper around `layout="icons"`.
